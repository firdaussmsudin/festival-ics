{
  "name": "festival-ics",
  "version": "0.1.0",
  "description": "Scrapes film festival dates, keeps a reviewable JSON dataset, and publishes a subscribable .ics feed.",
  "type": "module",
  "scripts": {
    "scrape": "tsx src/scrape.ts",
    "diff": "tsx src/diff.ts",
    "promote": "tsx src/promote.ts",
    "validate": "tsx src/validate.ts",
    "build:ics": "tsx src/generate-ics.ts"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "cheerio": "^1.0.0",
    "ics": "^3.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "tsx": "^4.16.2",
    "typescript": "^5.5.3"
  }
}
